import React from "react";

const Button = (props) => {
  
  return (
    <div>
      {/* <button className="add_button" onClick={props.addNewTask}>
     +
      </button> */}
    </div>
  );
};

export default Button;
